<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KnockOutArcade" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <!-- Tema personalizzato per il bottone -->
    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>  <!-- Sfondo nero -->
        <item name="android:textColor">#FFFFFF</item>  <!-- Testo bianco -->
    </style>
    <style name="Theme.KnockOutArcade" parent="Base.Theme.KnockOutArcade" />
</resources>